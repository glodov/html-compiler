doctype html
html(lang= locale)

  insert(block/__open.pug)

  body

    insert(block/header.pug)

    div.container
      h1= page.title

    ul
    	each item in products
    		li
    			p.lead= item.name
    			p= item.price

    insert(block/footer.pug)
  
  insert(block/__close.pug)